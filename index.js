// 1. Create a function expression called divide
const divide = function() {
    return 2000 / 100;
  }
  
  // 2. Create an arrow function called square
  const square = x => x * x;
  
  // 3. Create an arrow function called add
  const add = (a, b) => a + b;